;; inc / dec / abs / min / max (Phase 17.45 — backend mappings fixed)

(defn main []
  (println (inc 41))
  (println (dec 43))
  (println (abs -7))
  (println (abs 7))
  (println (min 3 7))
  (println (max 3 7))
  0)
