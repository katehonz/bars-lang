[package]
name = "bars-pkg"
version = "0.1.0"
edition = "2024"
authors = ["Bars Contributors"]
description = "Пакетен мениджър за Bars"
license = "MIT OR Apache-2.0"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
